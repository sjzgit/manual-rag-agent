# 增加关键词检索

1. 现在需要在系统中增加关键词检索，从而实现语义检索（当前已实现）+关键词检索的混合检索方式。
2. 结合项目现有逻辑和文档（`/docs`），帮我梳理一份技术方案

## 关键词检索

1. 向量化时需要增加稀疏向量，稀疏向量用于关键词检索
2. 将用户query 进行分词，然后进行BM25全文检索，这里需要你帮我补充技术细节，比如分词用什么模型等？
3. 语义检索和关键词检索的结果通过RRF或权重的方式融合，你结合我的业务场景和知识内容，帮我选一种合适的算法。我个人任务语义检索的权重要更高些
4. 将融合后的结果进行重排序，然后返回前5条结果。重排模型使用：`Qwen/Qwen3-Reranker-8B`，API文档地址：`https://api-docs.siliconflow.cn/docs/api/rerank-post`
5. 这次新增功能用到的新的模型配置加到配置文件中，我后面补充真实的配置上去

## 记录日志

1. 关键词检索也需要记录日志，并可以在会话详情中查看到
2. 需要与语义检索区分，要可以明确看到两种检索的结果列表（包含得分）
3. RRF 融合结果也需要记录到日志中，可以在会话详情中看到RRF 融合后的最终结果列表和得分
4. Rerank也需要记录到日志中，可以在会话详情中看到检索后Rerank的最终结果列表和得分
